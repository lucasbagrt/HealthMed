apiVersion: apps/v1
kind: Deployment
metadata:
  name: apiuser
spec:
  replicas: 3
  selector:
    matchLabels:
      app: apiuser
  template:
    metadata:
      labels:
        app: apiuser
    spec:
      containers:
        - name: apiuser
          image: healthmedfiap/fiap-user:latest
          ports:
            - containerPort: 82
